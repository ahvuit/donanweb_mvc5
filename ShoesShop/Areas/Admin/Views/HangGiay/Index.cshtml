@model IEnumerable<ShoesShop.Models.HANGGIAY>

@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_LayoutAdmin.cshtml";
}
<div class="card">
    <div class="card-header">
        <div class="row">
            <div class="col-md-6">
                <strong class="text-danger text-uppercase">Quản lý hãng giày</strong>
            </div>
            <div class="col-md-6 text-right">
                <a href="@Url.Action("Create","HangGiay")" class="btn btn-sm btn-success"> <i class="fas fa-plus"></i> Thêm Mới</a>
            </div>
        </div>
    </div>
    <div class="card-body">
        <table class="table">
            <tr>
                <th>
                    Tên hãng
                </th>
                <th>
                    Logo
                </th>
                <th></th>
            </tr>

            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.TenHang)
                    </td>
                    <td>
                        <img src="@Url.Content("~/Images/" +item.Logo)" width="50" height="50" />
                    </td>
                    <td>
                        <a href="@Url.Action("Edit", "HangGiay", new { id = item.MaHang })" class="btn btn-sm btn-info"> <i class="fas fa-edit"></i> Sửa </a>
                        <a href="@Url.Action("Details", "HangGiay", new { id = item.MaHang })" class="btn btn-sm btn-primary"> <i class="fas fa-eye"> Xem </i></a>
                        <a href="@Url.Action("Delete", "HangGiay", new { id = item.MaHang })" class="btn btn-sm btn-danger"> <i class="fas fa-trash"></i> Xóa </a>
                    </td>
                </tr>
            }

        </table>

    </div>
</div>
